# Skills

No custom skills are defined by default. Skills can be loaded dynamically at runtime using the `skill` tool.

To create a skill, add a markdown file to the `.skills/` directory in the project root with instructions for a specific task.
